import React from 'react';
import SolutionForm from "../solutionForm/solutionForm";

const SolutionVi = ({a, b, c, openSolution}) => {

    return (
        <SolutionForm a={a} b={b} c={c} openSolution={openSolution}>

        </SolutionForm>
    );
};

export default SolutionVi;